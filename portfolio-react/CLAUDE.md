## Projet en cours
Portfolio React (Vite) — conversion d'une maquette HTML/CSS statique.
Stack : React, Vite, pas de librairie CSS externe (styles inline ou CSS modules à définir).

Étapes prévues :
1. src/data/portfolioData.js — extraire skillGroups, projects, experience, 
   education, marqueeItems (avec id unique sur chaque objet itéré)
2. Components dans src/components/ : Nav, Hero, About, Skills, Work, 
   Experience, Education, Contact, Footer
3. Utilisation de .map() + key={item.id} pour les listes
4. Assembler le tout dans App.jsx

Mode : Socratique — pose des questions, ne donne jamais le code complet 
directement, laisse-moi raisonner et coder moi-même.